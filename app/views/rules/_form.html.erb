<%= form_for(@rule) do |f| %>
  <% if @rule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rule.errors.count, "error") %> prohibited this rule from being saved:</h2>

      <ul>
      <% @rule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="newRulePanel">

    <div class="field">
      <%= f.label :if, class: "label" %>
      <%= f.text_area :if, class: "textarea" %>
    </div>
    <div class="field">
      <%= f.label :then, class: "label" %>
      <%= f.text_area :then, class: "textarea" %>
    </div>
    <div class="field">
      <%= f.label :tag, class: "label" %>
      <%= f.text_area :tag, class: "textarea" %>
    </div>
    <div class="actions">
      <%= f.submit "ยืนยัน" %>
    </div>
  </div>
<% end %>
